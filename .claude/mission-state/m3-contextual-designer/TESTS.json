{
  "mission": "m3-contextual-designer",
  "status": "IMPLEMENTED_TESTS_IN_PROGRESS",
  "regression_gate": "M0, M1 and M2 suites (unit, integration, baseline, e2e) must stay green",
  "provider_policy": "Deterministic suites use the fake model provider. The live designer smoke (M3-T16) is required before final acceptance unless explicitly waived by the user (D13); it is pending until the Anthropic key is rotated.",
  "evidence": {
    "date": "2026-09-15",
    "commands": [
      "npm run build:packages — storefront-schema v3, storefront-core 0.3.0: pass",
      "apps/admin next build (Next.js 16.3.5): pass",
      "TEST_TYPE=unit jest (backend) — 13 suites, 218 tests passed (includes designer-contracts.unit.spec.ts)",
      "integration-tests/http/m3-designer.spec.ts — 12 tests passed",
      "PENDING: integration-tests/e2e/m3-designer-e2e.spec.ts (admin UI, real builds, screenshots)",
      "PENDING: full M0–M2 regression (unit, integration, baseline, e2e)"
    ],
    "database": "Docker Postgres m0-medusa-postgres on localhost:55432"
  },
  "tests": [
    { "id": "M3-T01", "requirement": "Selecting an element in the preview: the next designer turn receives that selection through the server-built ExecutionContext, re-resolved against the caller's own current draft (D6)", "status": "PASS", "evidence": "m3-designer.spec.ts › M3-T01 (server-resolved label/value in model input; ai_action.actor.selected_entity; stale selection re-resolved to null at execution)" },
    { "id": "M3-T02", "requirement": "Forged, foreign, stale or malformed element ids are rejected; the turn runs without selection or fails closed", "status": "PASS", "evidence": "m3-designer.spec.ts › M3-T02 (another store's element, wrong field, traversal, malformed, out-of-range, over-long: 404/400 and no run or message); designer-contracts resolveElement cases" },
    { "id": "M3-T03", "requirement": "Designer sessions and messages persist across refresh and worker process kill/restart, are StoreEnvironment-owned, and execute through the M2 lease/tool gate (D5)", "status": "PASS", "evidence": "m3-designer.spec.ts › M3-T03 (expired lease re-claimed, attempt 2, one revision, fresh read returns the conversation, rows environment-owned, completed turn never re-runs; a retried turn reuses the plan its first attempt stored, so operation replay by index cannot skip or misapply a change and no new model call is made). The M2 real-process kill test covers the shared worker." },
    { "id": "M3-T04", "requirement": "Every designer edit is a typed, audited, idempotent risk-0 tool call creating a new draft revision; invalid, extra-field or low-contrast output is rejected with no side effects (D12)", "status": "PASS", "evidence": "m3-designer.spec.ts › M3-T04 (audit rows, action_key = idempotency key, replay returns the same revision; unknown op, tenant key, unreadable palette, hero removal, missing section, over-long text all fail with no revision)" },
    { "id": "M3-T05", "requirement": "Revisions are immutable and append-only with a monotonic per-project sequence; concurrent edits on the same parent: one wins, one gets a conflict", "status": "PASS", "evidence": "m3-designer.spec.ts › M3-T05" },
    { "id": "M3-T06", "requirement": "Undo/restore to revision N creates a new revision equal to N and cancels in-flight designer turns for the project; a stale AI write cannot overwrite it (D8)", "status": "PASS", "evidence": "m3-designer.spec.ts › M3-T06 (undo appends restored_from; restore cancels a queued turn and a running turn stops at its checkpoint without editing)" },
    { "id": "M3-T07", "requirement": "Authenticated in-admin draft rendering reflects an edit quickly; promotion (risk 1) produces a real per-project preview build rendering the edited sections (D1, D9)", "status": "PASS_PENDING_E2E", "evidence": "m3-designer.spec.ts › M3-T07 (head config updated at once; promoted manifest v2 carries the revision; newest by sequence wins out of order). Real build rendering: m3-designer-e2e.spec.ts (pending)." },
    { "id": "M3-T08", "requirement": "Playwright/Chromium screenshot captured only from the server-derived preview URL, stored as tenant-owned media, recorded against the revision (D7)", "status": "UNIT_PASS_PENDING_E2E", "evidence": "designer-contracts › screenshot network policy; m3-designer-e2e.spec.ts › M3-T08 (pending)" },
    { "id": "M3-T09", "requirement": "Draft and preview content never appear on live hostnames or another store's preview; nothing goes live (D9)", "status": "PASS_PENDING_E2E", "evidence": "m3-designer.spec.ts › M3-T09; served-preview checks in m3-designer-e2e.spec.ts (pending)" },
    { "id": "M3-T10", "requirement": "Cross-tenant session, message, revision, screenshot, project and run ids return 404 on every new route and stream", "status": "PASS", "evidence": "m3-designer.spec.ts › M3-T10 (six routes incl. SSE and screenshots, no leakage; staff read-only; unauthenticated 401)" },
    { "id": "M3-T11", "requirement": "Prompt injection in merchant copy, storefront content or selected-element text cannot select a tenant, reach catalogue tools, set prices or stock, publish live, or emit HTML/CSS/JS", "status": "PASS", "evidence": "m3-designer.spec.ts › M3-T11 (markup stored as plain text; body tenant selector refused; foreign photo, live publish and product ops fail; other store untouched)" },
    { "id": "M3-T12", "requirement": "Schema v3 rejects unknown keys/sections, more than two variants, non-owned or URL media, javascript:/data: URLs; v2 upgrades to v3 losslessly (D4)", "status": "PASS", "evidence": "designer-contracts › schema v3 cases; storefront-contracts manifest media cases; m3-designer.spec.ts › M3-T12 (owned photo attached; smuggled foreign media fails the build)" },
    { "id": "M3-T13", "requirement": "Preview bridge postMessage accepts only the exact expected origins and windows; the preview gateway forbids framing; only the admin's own draft frame is framable, by the admin", "status": "UNIT_PASS_PENDING_E2E", "evidence": "designer-contracts › preview bridge; gateway-hardening › no-framing headers; m3-designer-e2e.spec.ts › M3-T13 admin headers and CORS (pending)" },
    { "id": "M3-T14", "requirement": "Per-store edit, designer turn, redeploy and screenshot rate limits enforced; monotonic deployment sequence settles newest preview; artifact path hardened to builds/<deployment_id>/out (D11)", "status": "PASS_PENDING_E2E", "evidence": "m3-designer.spec.ts › M3-T14 (turn, edit, preview deploy limits incl. M1 route 429); gateway-hardening exact artifact cases; screenshot limit in e2e (pending)" },
    { "id": "M3-T15", "requirement": "Designer UI meets DESIGN.md §6: Bulgarian copy, 375px and 1440px, keyboard and focus, contrast, reduced motion, all §3.5 states; verified in a real browser", "status": "PENDING_E2E", "evidence": "m3-designer-e2e.spec.ts (pending)" },
    { "id": "M3-T16", "requirement": "Opt-in live designer smoke: a real model applies a Bulgarian copy and theme edit to a selected element end to end", "status": "PENDING_CREDENTIALS_KEY_ROTATION", "evidence": "Opt-in spec integration-tests/live/m3-designer-smoke.spec.ts (skips without ANTHROPIC_API_KEY; run with npm run test:ai:live after key rotation): a real model edits the selected hero headline, and an injection turn must use designer tools only, change no prices and create no live deployment. Not run." },
    { "id": "M3-T17", "requirement": "M0, M1 and M2 regression suites pass (unit, integration, baseline, e2e)", "status": "PENDING" }
  ]
}
